FROM node:22-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package.json ./
RUN npm install

# Source is mounted as a volume at runtime — no COPY src needed for dev
EXPOSE 5173

CMD ["npm", "run", "dev"]
